Array_init:
  #空間サイズをraxに入れる
  push rdi #size引数
  push 8 #要素（整数）一つ当たりのサイズ
  pop rdi
  pop rax
  imul rdi

  push rbp
  mov rbp, rsp
  sub rsp, rax

  mov rax, rbp #thisにあたるポインタをraxで返している
  mov rsp, rbp
  pop rbp
  ret